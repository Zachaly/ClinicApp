export default interface PatientModel {
    id: string,
    peselNumber: string,
    firstName: string,
    lastName: string,
    birthDate: string,
    address: string,
    postalCode: string,
    city: string
}